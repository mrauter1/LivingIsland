# Plan ↔ Plan Verifier Feedback
- Added a single implementation phase because the accepted fixes are tightly coupled across kernel ID allocation, store-side selection, and clock math; documented `.gitignore` cleanup as conditional and deferred broad `handleWorldClick` refactoring due to central-path regression risk.
- PLAN-001 | blocking | `plan.md` narrows the ID fix to "editor-created entities and road nodes", but `src/simulation/core/engine.ts` also uses `nextId(...)` for runtime-created `event-*` records at lines 709, 743, 766, and 806. That leaves the same reuse bug in place after event expiry or replacement, so the plan does not yet cover the full review suggestion or all affected regression surfaces. Minimal correction: expand the plan and phase acceptance criteria/tests to require the monotonic/max-suffix helper for every `nextId` callsite, especially events, and add regression coverage for non-editor IDs as needed.
- Updated cycle 2 plan to resolve `PLAN-001` by widening the ID-allocation scope from editor-created entities to every `nextId(...)` consumer, explicitly including runtime-created `event-*` records in the plan narrative, phase scope, acceptance criteria, and validation strategy.
- PLAN-002 | non-blocking | Cycle 2 re-review found no remaining blocking gaps. `plan.md`, `phase_plan.yaml`, and `decisions.txt` now consistently require the ID strategy and regression coverage for all `nextId(...)` callsites, keep the clock-offset and `.gitignore` changes bounded, and defer broad `handleWorldClick` refactoring unless a tiny helper is directly needed.
