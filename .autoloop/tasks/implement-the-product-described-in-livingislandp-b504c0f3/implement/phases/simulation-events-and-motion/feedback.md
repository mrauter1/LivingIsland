# Implement ↔ Code Reviewer Feedback

- Task ID: implement-the-product-described-in-livingislandp-b504c0f3
- Pair: implement
- Phase ID: simulation-events-and-motion
- Phase Directory Key: simulation-events-and-motion
- Phase Title: Simulation loop, events, and representative motion
- Scope: phase-local authoritative verifier artifact

- IMP-001 `blocking` [src/simulation/core/engine.ts, `hydrateSave`; src/types/save.ts, `SAVE_SCHEMA_VERSION`]: this phase extends canonical world state with `runtime` and `traffic.highTrafficAverageCongestion`, but the save schema version remains `p0-v1` and `hydrateSave` does a raw clone with no normalization. Any local save created before this phase now hydrates without those required fields, which can break ticking/debug/inspection on load instead of invalidating the incompatible payload. Minimal fix: either bump the save schema version so older saves are rejected, or centrally backfill the new fields during hydration.
- IMP-002 `blocking` [src/simulation/core/engine.ts, `computeServiceCoverage`; src/world/rendering/WorldRenderer.ts, `updateActors`]: the PRD requires ferry efficiency penalties in rain and storm, but ferry contribution and ferry motion remain unchanged across weather states. `computeServiceCoverage` always grants the full `+14` ferry transport bonus, and renderer ferry actors never slow down in rain/storm. Concrete failure: a district served only by ferry gets the same transport benefit in clear weather and in a storm, so the mandated storm/rain ferry degradation never affects district outcomes or visible motion. Minimal fix: centralize ferry weather efficiency in the simulation core and feed the reduced value into both district transport scoring and ferry actor speed.
- IMP-003 `blocking` [src/simulation/core/engine.ts, `recalculateDistrictDemands`, `applyEventPenalties`, `updateEventState`; src/types/domain.ts, `District`]: traffic collapse and fire do not implement the PRD’s required efficiency losses. A burning district only has `transportDemand` zeroed, and traffic collapse only adds satisfaction/growth penalties; neither event reduces an inspectable district efficiency/output metric, so commercial/industrial districts continue contributing full jobs/demand as though they were operating normally. Concrete failure: a commercial district under traffic collapse or an industrial district on fire still reports normal population/jobs-derived demand behavior aside from generic penalties, which misses the required “lose efficiency” / “drops to 0” rule. Minimal fix: add a central operational-efficiency multiplier to district simulation state/derivation, apply fire=`0` and traffic-collapse reduction there, and expose it in the inspector/debug surfaces.
- IMP-004 `non-blocking` Re-review note: verified that `p0-v2` now invalidates older save payloads at the schema gate, ferry transport/motion penalties are weather-conditioned and route-backed, and district operational efficiency is exposed through simulation, inspector, and debug surfaces for fire and traffic-collapse effects. Targeted validation passed via `npm run test -- --run src/simulation/core/engine.test.ts src/app/store/appStore.test.ts src/ui/components/WorldViewport.test.tsx`, `npm run lint`, and `npm run build`. The remaining Vite large-chunk warning is unchanged and not a phase blocker.
